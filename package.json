{
  "name": "daily-command-center",
  "version": "1.0.0",
  "description": "Drake's Daily Command Center \u2014 standalone Express dashboard",
  "main": "server.js",
  "scripts": {
    "check:db": "node scripts/check-db.js",
    "db:refresh-local": "powershell -ExecutionPolicy Bypass -File scripts/refresh-local-db.ps1",
    "db:schedule-local-refresh": "powershell -ExecutionPolicy Bypass -File scripts/refresh-local-db.ps1 -InstallDailyTask",
    "brief:materialize": "node scripts/morning-brief-materializer.js",
    "triage:ingest": "node scripts/triage-check-ingest.js",
    "sweep:check": "node scripts/sweep-suite-check.js",
    "ship": "node scripts/ship-pr.js",
    "test": "node --test",
    "lint": "eslint .",
    "format": "prettier --write .",
    "start": "npm run check:db && node pg-schema.js && node server.js",
    "dev": "npm run start",
    "start:prod": "node pg-schema.js && node server.js",
    "schema": "node pg-schema.js",
    "backup": "bash scripts/db-backup.sh"
  },
  "engines": {
    "node": ">=20 <23"
  },
  "dependencies": {
    "@clerk/backend": "^3.5.0",
    "@clerk/express": "^2.1.23",
    "bcryptjs": "^3.0.3",
    "chokidar": "^3.6.0",
    "connect-pg-simple": "^10.0.0",
    "dotenv": "^16.4.0",
    "express": "^4.21.0",
    "express-session": "^1.19.0",
    "googleapis": "^171.4.0",
    "gray-matter": "^4.0.3",
    "pg": "^8.13.0",
    "simple-git": "^3.25.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.3.0"
  }
}
