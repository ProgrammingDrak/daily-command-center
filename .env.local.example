# Optional local-only secrets. Copy to .env.local and fill in.
# .env.local is gitignored.

# Remote Postgres connection string used by:
#   npm run db:refresh-local -- -Yes
DCC_REMOTE_DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DATABASE
